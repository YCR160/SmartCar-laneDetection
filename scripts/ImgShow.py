import cv2


def ShowImg(img1, H):
    """
    对输入的图像进行透视变换，并显示变换后的图像
    :param img1: 输入图像
    :param H: 透视变换矩阵
    :return: 无
    """
    # img2 = cv2.warpPerspective(img1, H, (200,200))
    # cv2.imshow('2', img1)
    # cv2.waitKey(1)
    # cv2.imshow('1', img2)
    # cv2.waitKey(1)
    return
